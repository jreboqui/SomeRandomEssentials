//There's many ways of doing this...
//given a string

String str; 
//did something to store a string in str

for (int i = str.length()-1; i >= 0; i--)
              {
                  std::cout << str[i] << " ";
              }
              std::cout << std::endl;
    
